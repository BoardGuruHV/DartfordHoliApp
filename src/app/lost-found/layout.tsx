import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Lost & Found | Dartford Holi Festival 2025",
  description: "Report or find lost items at the Dartford Holi Festival. Check the lost and found list or submit a report.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
