import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Preparation Checklist | Dartford Holi Festival 2025",
  description: "Everything you need to bring to the Dartford Holi Festival. Track your preparation with our interactive checklist.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
