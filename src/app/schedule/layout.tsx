import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Schedule | Dartford Holi Festival 2025",
  description: "Full programme schedule for the Dartford Holi Festival — ceremonies, performances, activities & more. Sunday 8th March, 10:30 AM – 2:30 PM.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
